# Traefik Static Configuration - HTTP Only (No SSL)
api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: "tasktracker-network"

log:
  level: INFO

accessLog: {}
